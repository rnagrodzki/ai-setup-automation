---
description: Check openspec CLI availability, project initialization, and version currency
allowed-tools: [Read, Glob, Grep, Bash, Skill, TodoWrite]
---

# /spec-check Command

Check openspec CLI availability, project initialization status, and version currency.
Suggests remediation (install, init, or update) with user confirmation.

## Usage

- `/aisa:spec-check` — Run the openspec availability check

## Workflow

Invoke the `aisa-openspec-checker` skill.
