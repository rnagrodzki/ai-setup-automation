## Project: no-setup

This is a Node.js project with the following structure:

```
src/
  index.js
  utils.js
package.json
README.md
```

There is NO `.claude/` directory — no skills, no agents, no cache, no learnings log. The project has never had AI setup automation applied.
